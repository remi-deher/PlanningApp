name: Build Unsigned IPA

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install XcodeGen
      run: brew install xcodegen

    - name: Generate Xcode Project
      run: xcodegen generate
      working-directory: .

    - name: Build App
      run: |
        xcodebuild -project PlanningApp.xcodeproj \
                   -scheme PlanningApp \
                   -configuration Release \
                   -sdk iphoneos \
                   clean build \
                   SYMROOT=build \
                   INFOPLIST_FILE=PlanningApp/Info.plist \
                   CODE_SIGNING_ALLOWED=NO \
                   CODE_SIGNING_REQUIRED=NO \
                   CODE_SIGN_IDENTITY=""
      working-directory: .

    - name: Verify UIRequiresFullScreen in built IPA
      run: |
        echo "=== Checking Info.plist in built .app ==="
        plutil -p build/Release-iphoneos/PlanningApp.app/Info.plist | grep -i "UIRequiresFullScreen" && echo "✅ UIRequiresFullScreen is PRESENT" || echo "❌ UIRequiresFullScreen is MISSING"
        plutil -p build/Release-iphoneos/PlanningApp.app/Info.plist | grep -i "UILaunchScreen" && echo "✅ UILaunchScreen is PRESENT" || echo "❌ UILaunchScreen is MISSING"
      working-directory: .

    - name: Package IPA
      run: |
        mkdir -p build/Payload
        cp -r build/Release-iphoneos/PlanningApp.app build/Payload/
        cd build
        zip -r PlanningApp.ipa Payload
      working-directory: .

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: PlanningApp-Unsigned
        path: build/PlanningApp.ipa
